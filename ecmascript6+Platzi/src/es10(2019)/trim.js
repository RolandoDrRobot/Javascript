// Eliminates the spaces at the start
let hello = '        Hello World';
console.log(hello);
console.log(hello.trimStart());

// Eliminates the spaces at the end
let hello = 'Hello World        ';
console.log(hello);
console.log(hello.trimEnd());
